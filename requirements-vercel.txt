# Vercel-specific requirements for Healthcare Assistant
# Optimized for serverless deployment

# Core ML and Data Science (lightweight versions)
scikit-learn==1.3.0
pandas==2.0.3
numpy==1.24.3
xgboost==2.0.3

# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0

# Data Processing
requests==2.31.0
joblib==1.3.2

# Note: Streamlit is not included as it doesn't work well with Vercel
# Use the API endpoints instead
